eureka:
  client:
    service-url:
      defaultZone: http://peer1:8761/eureka/,http://peer2:8762/eureka/

spring:
  application:
    name: config-client
  cloud:
    config:
      profile: dev
      uri: http://localhost:8888/

server:
  port: 8881